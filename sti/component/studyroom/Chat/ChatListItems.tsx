// @ts-nocheck

import React from 'react';

interface Test {}

const ChatListItems: Test = ({ data }) => {
  return (
    <div>
      <span>{data}</span>
    </div>
  );
};

export default ChatListItems;
